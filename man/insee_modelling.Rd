% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-insee-regressors.R
\name{insee_modelling}
\alias{insee_modelling}
\alias{create_french_calendar}
\alias{create_insee_regressors}
\alias{create_insee_regressors_sets}
\alias{create_insee_context}
\title{Régressions et contexte de modélisation INSEE}
\usage{
create_french_calendar()

create_insee_regressors(
  start = c(1990L, 1L),
  frequency = 12L,
  length = 492L,
  s = NULL
)

create_insee_regressors_sets(
  start = c(1990L, 1L),
  frequency = 12L,
  length = 492L,
  s = NULL
)

create_insee_context(
  start = c(1990L, 1L),
  frequency = 12L,
  length = 492L,
  s = NULL
)
}
\arguments{
\item{start}{[\link[base]{integer} vector] Période de début au format \code{c(année, mois)} (par défaut \code{c(1990, 1)}).}

\item{frequency}{\link{integer} Fréquence de la série (par défaut \code{12L}).}

\item{length}{\link{integer} Longueur de la série (par défaut \code{492L}).}

\item{s}{[\link[base]{numeric} or NULL] Argument optionnel pour ajustement (passé à \code{rjd3toolkit}).}
}
\value{
\itemize{
\item \code{create_french_calendar()} retourne un objet \code{national_calendar}.
\item \code{create_insee_regressors()} retourne une matrice de régressions (jours ouvrés + LY).
\item \code{create_insee_regressors_sets()} retourne une liste d’ensembles de régressions (\code{REG1}, \code{REG2}, …, \code{REG6}, avec ou sans LY).
\item \code{create_insee_context()} retourne un objet \code{modelling_context}.
}
}
\description{
Ces fonctions permettent de construire les régressions et le contexte
standard utilisés par l’INSEE pour la désaisonnalisation :
\itemize{
\item \code{\link[=create_french_calendar]{create_french_calendar()}} crée le calendrier national français.
\item \code{\link[=create_insee_regressors]{create_insee_regressors()}} génère les régressions jours ouvrés et effet année bissextile.
\item \code{\link[=create_insee_regressors_sets]{create_insee_regressors_sets()}} organise ces régressions en ensembles standards (REG1, REG2, …, REG6, avec ou sans LY).
\item \code{\link[=create_insee_context]{create_insee_context()}} combine les régressions et le calendrier dans un objet \code{modelling_context} utilisable directement avec \code{rjd3toolkit}.
}
}
\examples{
# 1. Créer le calendrier français
cal <- create_french_calendar()
cal

# 2. Générer les régressions
regs <- create_insee_regressors(start = c(2000, 1), frequency = 12, length = 240)
head(regs)

# 3. Organiser en ensembles standardisés
sets <- create_insee_regressors_sets(start = c(2000, 1), frequency = 12, length = 240)
names(sets)

# 4. Construire un contexte complet pour rjd3toolkit
ctx <- create_insee_context(start = c(2000, 1), frequency = 12, length = 240)
ctx

}
