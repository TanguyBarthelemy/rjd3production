% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/affect-outliers.R
\name{affect_outliers}
\alias{affect_outliers}
\title{Assign outliers to a Demetra+ workspace}
\usage{
affect_outliers(outliers, ws_path)
}
\arguments{
\item{outliers}{[\link[base]{list}] A named list where each element corresponds
to a series in the workspace.
Each element should be a character vector of outlier specifications
(e.g. \code{"AO (2020-03)"}, \code{"LS (2008-09)"}).}

\item{ws_path}{[\link[base]{character}] Path to a Demetra+ workspace file
(usually with extension \code{.xml}).}
}
\value{
Invisibly returns \code{NULL}. The workspace file at \code{ws_path} is updated on disk
with the new outliers.
}
\description{
This function updates a Demetra+ workspace (\code{.xml}) by inserting
pre-specified outliers into the \code{domainSpec} of each seasonal adjustment
model (SAI).
Typically, the list of outliers is created with \code{\link[=retrieve_outliers]{retrieve_outliers()}} and
stored/exported with \code{\link[=export_outliers]{export_outliers()}}.
}
\details{
For each series in the workspace:
\itemize{
\item The function looks up the corresponding entry in the \code{outliers} list.
\item If outliers are specified, they are split into \strong{type} (\code{AO}, \code{LS}, \code{TC}, …)
and \strong{date} (e.g. \code{"2020-03"}) using regex patterns.
\item These outliers are then added into the \code{domainSpec} of the model using
\code{\link[=add_outlier]{add_outlier()}}.
}

The modified workspace is saved \strong{in place} (argument \code{replace = TRUE}).
}
\examples{
\dontrun{
# Retrieve outliers from an existing workspace
outs <- retrieve_outliers("workspace.xml")

# Reapply them to another workspace
affect_outliers(outliers = outs, ws_path = "workspace.xml")
}

}
\seealso{
\itemize{
\item \code{\link[=retrieve_outliers]{retrieve_outliers()}} to extract outliers from an existing workspace.
\item \code{\link[=export_outliers]{export_outliers()}} / \code{\link[=import_outliers]{import_outliers()}} to manage YAML files of outliers.
}
}
