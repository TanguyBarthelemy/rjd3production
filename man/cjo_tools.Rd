% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R, R/retrieve.R
\name{cjo_tools}
\alias{cjo_tools}
\alias{export_cjo}
\alias{import_cjo}
\alias{retrieve_cjo}
\title{Manage working-day regressors (CJO) from JDemetra+ workspaces}
\usage{
export_cjo(x, ws_name, path = NULL, verbose = TRUE)

import_cjo(x, ws_name, path = NULL, verbose = TRUE)

retrieve_cjo(ws_path)
}
\arguments{
\item{x}{[\link[base]{list} | \link[base]{data.frame}] An object containing
the CJO information, typically the output of \code{\link[=retrieve_cjo]{retrieve_cjo()}}.}

\item{ws_name}{[\link[base]{character}] The name of the workspace,
used to build default YAML filenames.}

\item{path}{[\link[base]{character}] Path to a YAML file to write to
or read from. If missing, defaults to
\code{"regression/cjo_<ws_name>.yaml"}.}

\item{verbose}{[\link[base]{logical}] Whether to print informative
messages (default: \code{TRUE}).}

\item{ws_path}{[\link[base]{character}] Path to a JDemetra+ workspace
file (usually with extension \code{.xml}).}
}
\value{
\itemize{
\item \code{retrieve_cjo()} returns a \link{data.frame} with columns:
\itemize{
\item \code{series}: series names,
\item \code{regs}: CJO regressor specification (\code{REG1}, \code{REG2}, …, \code{REG6},
with or without \verb{_LY}),
\item \code{series_span}: currently empty but reserved for series span.
}
\item \code{export_cjo()} invisibly returns the path of the YAML file written.
\item \code{import_cjo()} returns a list or data structure read from YAML.
}
}
\description{
These functions allow extracting, exporting, and importing the
\emph{calendrier jours ouvrés} (CJO) regressors used in JDemetra+ workspaces:
\itemize{
\item \code{\link[=retrieve_cjo]{retrieve_cjo()}} extracts the CJO specification from a \code{.xml} workspace.
\item \code{\link[=export_cjo]{export_cjo()}} saves extracted CJO information into a YAML file.
\item \code{\link[=import_cjo]{import_cjo()}} loads CJO information back from a YAML file.
}

They are useful for documenting and reusing the regression settings
applied in seasonal adjustment workflows.
}
\examples{
\dontrun{
ws_file <- "path/to/workspace.xml"

# 1. Retrieve CJO specification
cjo <- retrieve_cjo(ws_file)

# 2. Export to YAML
export_cjo(cjo, ws_name = "workspace1")

# 3. Import back from YAML
imported <- import_cjo(x = NULL, ws_name = "workspace1")
}

}
