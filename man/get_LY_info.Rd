% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{get_LY_info}
\alias{get_LY_info}
\title{Diagnostics Extraction on Calendar Correction with different sets of regressors}
\usage{
get_LY_info(mod, verbose = TRUE)
}
\arguments{
\item{mod}{\link{list} An X13 model.}

\item{verbose}{Boolean indicating whether to print additional information.
Default is \code{TRUE}.}

\item{series}{[\link[stats]{ts} or numeric] Time series to analyse.}

\item{spec}{\link{list} A X13 specification (from \code{\link[rjd3x13:x13_spec]{rjd3x13::x13_spec()}}).}

\item{context}{\link{list} Modelling context with regressors and calendars
(from \code{\link[rjd3toolkit:modelling_context]{rjd3toolkit::modelling_context()}}).}

\item{jeu}{\link{character} Name of the tested regression set.}

\item{diags}{\link{data.frame} Diagnostics table produced by \code{all_diagnostics()}.}

\item{name}{\link{character} Name of the series (for messages).}

\item{specs_set}{[\link[base]{list} or NULL] List of X13 specifications. If
\code{NULL}, generated via \code{\link[=create_specs_set]{create_specs_set()}}.}

\item{...}{Additional arguments passed to \code{\link[=create_specs_set]{create_specs_set()}} controlling
the generation of X13 specifications. Possible arguments include:
\describe{
\item{outliers}{Optional list of outliers with elements \code{type} (vector
of types, e.g., "AO", "LS", "TC") and \code{date} (vector of dates).}
\item{span_start}{Starting date of the estimation (character, format
\code{"YYYY-MM-DD"}).}
\item{...}{Other arguments accepted by \code{\link[=create_specs_set]{create_specs_set()}}.}
}}

\item{thresholds}{The thresholds used to compare the different model and
set a grade to an adjustment based on the td residuals p-value and leap
year significancy.}
}
\value{
\itemize{
\item \code{get_LY_info()} : A data.frame with \code{LY_coeff} and \code{LY_p_value}.
\item \code{one_diagnostic()} : A data.frame with diagnostics for one specification.
\item \code{all_diagnostics()} : A data.frame with diagnostics for all specifications.
\item \code{verif_LY()} : Name of the chosen regression set (possibly without LY).
\item \code{select_td_one_series()} : Name of the selected regression set.
}
}
\description{
These functions allow to extract diagnostics from X13-Arima models with
different sets of calendar regressors in order to evaluate different
specifications and select the most appropriate calendar regressors set (with
or without leap-year effect) to correct a given series.
}
\details{
\itemize{
\item \code{get_LY_info()} extracts coefficient and p-value of the leap-year (LY)
effect.
\item \code{one_diagnostic()} applies one X13 specification to a series and computes
diagnostics.
\item \code{all_diagnostics()} evaluates all specifications in a set and summarizes
diagnostics.
\item \code{verif_LY()} checks whether the leap-year effect should be kept or removed.
\item \code{select_td_one_series()} selects the best calendar regressors set for a
single series.
}
}
\examples{
\dontshow{if (rjd3jars::check_java_version(silent = TRUE)) withAutoprint(\{ # examplesIf}
library("rjd3toolkit")

# Create a modelling context
my_context <- create_insee_context(s = ABS)

# Generate specification sets
my_set <- create_specs_set(context = my_context)

# Extract LY info
mod <- rjd3x13::x13(ABS[, 1], spec = "RSA3")
rjd3production:::get_LY_info(mod)

# Compute diagnostics for one spec
spec <- my_set[[8L]]
rjd3production:::one_diagnostic(series = ABS[, 1], spec, context = my_context)

# Compute diagnostics for all specs
rjd3production:::all_diagnostics(
    series = ABS[, 1],
    specs_set = my_set,
    context = my_context
)

# Check whether LY should be removed
diags <- rjd3production:::all_diagnostics(
    series = ABS[, 1],
    specs_set = my_set,
    context = my_context
)
rjd3production:::verif_LY("REG6_LY", diags)

# Select regressions for one series
rjd3production:::select_td_one_series(series = ABS[, 1], context = my_context)
\dontshow{\}) # examplesIf}
}
\keyword{internal}
