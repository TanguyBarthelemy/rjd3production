% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select-cjo.R
\name{diagnostics_selection}
\alias{diagnostics_selection}
\alias{get_LY_info}
\alias{one_diagnostic}
\alias{all_diagnostics}
\alias{verif_LY}
\alias{select_reg_one_series}
\title{Diagnostics and Regression Selection for X13 Models}
\usage{
get_LY_info(smod, verbose = TRUE)

one_diagnostic(series, spec, context)

all_diagnostics(series, specs_set, context)

verif_LY(jeu, diags)

select_reg_one_series(series, name = "", specs_set = NULL, context = NULL, ...)
}
\arguments{
\item{smod}{\link{list} Result of \code{\link[=summary]{summary()}} applied to an X13 model.}

\item{series}{[\link[stats]{ts} or numeric] Time series to analyze.}

\item{spec}{\link{list} An X13 specification (from \code{\link[rjd3x13:x13_spec]{rjd3x13::x13_spec()}}).}

\item{context}{\link{list} Modelling context with regressors and calendars
(from \code{\link[rjd3toolkit:modelling_context]{rjd3toolkit::modelling_context()}}).}

\item{specs_set}{[\link[base]{list} or NULL] List of X13 specifications. If \code{NULL},
generated via \code{\link[=create_specs_set]{create_specs_set()}}.}

\item{jeu}{\link{character} Name of the tested regression set.}

\item{diags}{\link{data.frame} Diagnostics table produced by \code{\link[=all_diagnostics]{all_diagnostics()}}.}

\item{name}{\link{character} Name of the series (for messages).}

\item{...}{Additional arguments passed to \code{\link[=create_specs_set]{create_specs_set()}} controlling
the generation of X13 specifications. Possible arguments include:
\describe{
\item{outliers}{Optional list of outliers with elements \code{type} (vector of types, e.g., "AO", "LS", "TC") and \code{date} (vector of dates).}
\item{span_start}{Starting date of the estimation (character, format \code{"YYYY-MM-DD"}).}
\item{...}{Other arguments accepted by \code{\link[=create_specs_set]{create_specs_set()}}.}
}}
}
\value{
\itemize{
\item \code{get_LY_info()} : A data.frame with \code{LY_coeff} and \code{LY_p_value}.
\item \code{one_diagnostic()} : A data.frame with diagnostics for one specification.
\item \code{all_diagnostics()} : A data.frame with diagnostics for all specifications.
\item \code{verif_LY()} : Name of the chosen regression set (possibly without LY).
\item \code{select_reg_one_series()} : Name of the selected regression set.
}
}
\description{
These functions provide tools to extract diagnostics from X13 models,
evaluate sets of specifications, and select the most appropriate
regression set (with or without leap-year effect).
}
\details{
\itemize{
\item \code{get_LY_info()} extracts coefficient and p-value of the leap-year (LY) effect.
\item \code{one_diagnostic()} applies one X13 specification to a series and computes diagnostics.
\item \code{all_diagnostics()} evaluates all specifications in a set and summarizes diagnostics.
\item \code{verif_LY()} checks whether the leap-year effect should be kept or removed.
\item \code{select_reg_one_series()} selects the best regression set for a single series.
}
}
\examples{
# Create a modelling context
my_context <- create_insee_context(s = AirPassengers)

# Generate specification sets
my_set <- create_specs_set(context = my_context)

# Extract LY info
mod <- rjd3x13::x13(AirPassengers, spec = "RSA3")
rjd3production:::get_LY_info(summary(mod))

# Compute diagnostics for one spec
spec <- my_set[[8L]]
rjd3production:::one_diagnostic(series = AirPassengers, spec, context = my_context)

# Compute diagnostics for all specs
rjd3production:::all_diagnostics(series = AirPassengers, specs_set = my_set, context = my_context)

# Check whether LY should be removed
diags <- rjd3production:::all_diagnostics(
    series = AirPassengers,
    specs_set = my_set,
    context = my_context
)
rjd3production:::verif_LY("REG6_LY", diags)

# Select regressions for one series
rjd3production:::select_reg_one_series(series = AirPassengers, context = my_context)

}
\keyword{internal}
