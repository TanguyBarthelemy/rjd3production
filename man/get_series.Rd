% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{get_series}
\alias{get_series}
\title{Extract all series from a SAI}
\usage{
get_series(jsai)
}
\arguments{
\item{jsai}{A Java Seasonal Adjustment Item object, typically obtained via
\code{\link[=jsap_sai]{jsap_sai()}} after opening and computing a workspace with \code{\link[=jws_open]{jws_open()}}
and \code{\link[=jws_compute]{jws_compute()}}.}
}
\value{
A \code{data.frame} with columns:
\itemize{
\item \code{SAI}: name of the SAI,
\item \code{series}: the type of series (e.g. \code{"y"}, \code{"sa"}, \code{"trend"}),
\item \code{date}: observation dates,
\item \code{value}: numeric values of the series.
}
}
\description{
Extracts all available time series (pre-adjustment, decomposition, and final)
from a seasonal adjustment item (\code{jsai}) inside a Demetra+ workspace.
}
\examples{
\dontrun{
library("rjd3workspace")
path <- file.path(tempdir(), "workspace_RSA3.xml")
jws <- jws_open(path)
jws_compute(jws)
jsap <- jws_sap(jws, 1L)
jsai <- jsap_sai(jsap, 1L)

df <- get_series(jsai)
head(df)
}

}
